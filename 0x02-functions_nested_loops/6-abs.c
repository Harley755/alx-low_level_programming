#include <ctype.h>
#include "main.h"
/**
 * _abs - get absolute value of an integer
 * @c: the character to be checked with function abs() include in c
 * Return: A absolute value of an integer
 */
int _abs(int c)
{
	return (abs(c));
}
