#include <stdio.h/>
/**
 * main - main function (entry point)
 * used printf function to display text
 * Return: 0 for success
 */
int main(void)
{
		printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
